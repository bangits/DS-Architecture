{
  "name": "@my-ui/core",
  "version": "12.0.10",
  "main": "./index.js",
  "types": "./index.d.ts",
  "license": "MIT",
  "scripts": {
    "deploy": "cd dist && npm publish",
    "bundle": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "copy": "cp package.json dist/ && cp ../../.npmrc dist/",
    "build": "npm run bundle && npm run copy",
    "lint": "eslint \"**/*.ts*\""
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "tsc-alias": "^1.8.7",
    "tsconfig": "*",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "@vanilla-extract/css": "^1.12.0",
    "@vanilla-extract/recipes": "^0.5.0"
  },
  "peerDependencies": {
    "@vanilla-extract/css": "^1.12.0"
  }
}
