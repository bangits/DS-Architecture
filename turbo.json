{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "inputs": ["src/**/*.tsx", "src/**/*.ts"]
    },
    "lint": {},
    "deploy": {
      "inputs": ["src/**/*.tsx", "src/**/*.ts"],
      "dependsOn": ["semantic-release", "build"],
      "persistent": true
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
